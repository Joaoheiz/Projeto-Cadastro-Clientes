package CadastroDeCliente;

public class AnotacoesCodigos {

	/* Codigo keyPressed !!!
	
	if(e.getKeyCode() == KeyEvent.VK_TAB) {
	      // Enter was pressed. Your code goes here.
	   }
	
	
	/* Codigo deixa o texto tudo maiusculo !!!
	
	Codigo_Principal.setText(Codigo_Principal.getText().toUpperCase(getLocale()));
	
	
	/* Codigo para Validar um campo Obrigatorio!!!
	
	if (Codigo_Principal.getText().equals("")) { 
		JOptionPane.showMessageDialog(null, "Favor preencher o nome."); 
			return; 
		}
		
	/* CODIGO LIMITAR CARACTERES !!!!!!!!
	 * 
	 * @Override
			public void keyTyped(KeyEvent e) {
				if(NomeGrupo_Principal.getText().length() >= 80) {
					e.consume();
				}
			}
	 */
}
